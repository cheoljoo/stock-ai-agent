# AI 제공자 선택: gemini(기본값) 또는 claude
# agy CLI가 이미 두 계정을 모두 지원하므로 값만 바꾸면 전환됩니다.
AI_PROVIDER=gemini

# agy가 인식하는 정확한 모델명을 직접 지정하고 싶을 때만 설정 (미설정 시 provider별 기본값 사용)
# 예: gemini-3.1-pro-high, claude-sonnet-4-6
# AI_MODEL_ID=gemini-3.1-pro-high

# agy CLI 응답 대기 제한 시간(초)
# AI_CLI_TIMEOUT=300
